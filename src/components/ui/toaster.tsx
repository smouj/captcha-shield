"use client"

import { Toast } from "@/components/ui/toast"

export function Toaster() {
  // Simplified toaster — the full Radix UI toast primitives are not available.
  // The use-toast hook and this component are kept for compatibility but
  // are not currently used in the application.
  return null
}
